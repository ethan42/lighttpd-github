name: Mayhem
on:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: '${{ matrix.os }} shared=${{ matrix.shared }} ${{ matrix.build_type }}'
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        shared: [false]
        build_type: [Release]
        include:
          - os: ubuntu-latest
            triplet: x64-linux

    steps:
      - uses: actions/checkout@v2

      - name: Start analysis
        uses: ethan42/mcode-action@4a0b6f1935ceec04870d541313664a32a30c82de
        with:
          mayhem-token: ${{ secrets.MAYHEM_TOKEN }}
          args: --image ethan42/lighttpd:pr-16 --corpus file://mayhem/corpus
          sarif-output: sarif
