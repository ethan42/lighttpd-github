name: Mayhem
on:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: '${{ matrix.os }} shared=${{ matrix.shared }} ${{ matrix.build_type }}'
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        shared: [false]
        build_type: [Release]
        include:
          - os: ubuntu-latest
            triplet: x64-linux

    steps:
      - uses: actions/checkout@v2

      - name: Start analysis
        uses: ethan42/mcode-action@0a4c62876814b41c8b24fd5338cfbf873d4c9e26
        with:
          mayhem-token: ${{ secrets.MAYHEM_TOKEN }}
          args: --image ethan42/lighttpd:pr-16 --corpus file://mayhem/corpus
          sarif-output: sarif


      - name: Upload SARIF file(s)
        uses: github/codeql-action/upload-sarif@v1
        with:
          sarif_file: sarif
